{
  "name": "@stent/proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Stent reverse proxy — multi-publisher x402 paywall over Circle Gateway batched settlement.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@circle-fin/x402-batching": "^3.2.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "vitest": "^3.2.0"
  }
}
